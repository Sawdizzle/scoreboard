# Pending migrations

Written, reviewed, and **not applied** to the live project. Each file says what
has to be true before it can run. When one is applied, move it into
`../migrations/` unchanged, so that folder stays the record of what the
database actually has.
