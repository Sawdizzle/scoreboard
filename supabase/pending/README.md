# Pending migrations

Written, reviewed, and **not applied** to the live project. Each file says what
has to be true before it can run. Once one is applied, move it into
`../migrations/`, renamed to the version the remote history recorded for it,
so that folder stays the record of what the database actually has.

Nothing is pending right now.
